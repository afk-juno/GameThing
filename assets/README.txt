Optional audio files:

  incoming.mp3  - mission-start alert (~11s after shaving first/last 2s). Missiles wait until it ends.
  fire.mp3      - optional fire loop. If missing, built-in gatling synth is used.
