Optional audio files:

  incoming.mp3  - mission-start alert (trimmed ~15s: alarm + INCOMING x3). Missiles wait until it ends.
  fire.mp3      - optional fire loop. If missing, built-in gatling synth is used.
